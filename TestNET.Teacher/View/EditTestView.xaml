<UserControl 
    x:Class="TestNET.Teacher.View.EditTestView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:local="clr-namespace:TestNET.Teacher.View"
	xmlns:model="clr-namespace:TestNET.Shared.Model;assembly=TestNET.Shared"
	xmlns:sharedserv="clr-namespace:TestNET.Shared.Service;assembly=TestNET.Shared"
	xmlns:viewmodel="clr-namespace:TestNET.Teacher.ViewModel" 
    xmlns:serv="clr-namespace:TestNET.Teacher.Service"
    xmlns:latex="clr-namespace:WpfMath.Controls;assembly=WpfMath"
    xmlns:CustomControls="clr-namespace:TestNET.Shared.CustomControls;assembly=TestNET.Shared"
    d:DataContext="{d:DesignInstance Type=viewmodel:EditTestViewModel}"
    mc:Ignorable="d" 
    d:DesignHeight="450" d:DesignWidth="800">

    <DockPanel>

        <Menu DockPanel.Dock="Top">
            <!--Grid.Column="4"-->
            <MenuItem Header="{DynamicResource back}"
                              Command="{Binding BackCommand}"/>
            <MenuItem Header="{DynamicResource addQuestion}">
                <MenuItem Header="{DynamicResource shortAnswer}"
                                  Command="{Binding AddSAQuestionCommand}" />
                <MenuItem Header="{DynamicResource multipleChoice}"
                                  Command="{Binding AddMCQuestionCommand}" />
                <MenuItem Header="{DynamicResource multipleChoiceManyCorrect}"
                                  Command="{Binding AddMCMQuestionCommand}" />
            </MenuItem>
            <MenuItem Header="{DynamicResource save}"
                              Command="{Binding SaveCommand}"/>
            <MenuItem Header="{DynamicResource cancel}"
                              Command="{Binding CancelCommand}"/>
        </Menu>

        <ScrollViewer>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition />
                </Grid.RowDefinitions>
                    <!--
                <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50" />
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="50" />
                    <ColumnDefinition Width="50" />
                    <ColumnDefinition Width="100" />
                </Grid.ColumnDefinitions>
                <Button Content="Back" 
                        Grid.Column="0"
                        Command="{Binding BackCommand}" 
                        IsEnabled="{Binding IsNotBusy}"/>
                <Button Content="Add Question"
                        Grid.Column="1"
                        Command="{Binding AddQuestionCommand}"/>
                <Button Content="Save"
                        Grid.Column="2"
                        Command="{Binding SaveChangesCommand}"/>
                <Button Content="Cancel"
                        Grid.Column="3"
                        Command="{Binding CancelCommand}"/>
                </Grid>
                -->
                <WrapPanel Grid.Row="0">
                    <Label Content="{DynamicResource testName}"/>
                    <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Grid.Row="1"
                         VerticalContentAlignment="Center"
                             MinWidth="150"/>
                </WrapPanel>
                <ListView Grid.Row="1" 
                          ItemsSource="{Binding Questions}"
                          HorizontalContentAlignment="Stretch"
                          SelectedIndex="{Binding SelIndex}"
                          PreviewMouseWheel="ListView_PreviewMouseWheel">
                    <ListView.Resources>

                        <sharedserv:IndexConverter x:Key="IndexConverter" />
                        
                        <!--

                        <DataTemplate DataType="{x:Type model:ShortAnswerQuestion}">

                            <CustomControls:QuestionPanel QuestionIndex="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListViewItem}}, Converter={StaticResource IndexConverter}}">
                                <StackPanel Style="{StaticResource QuestionBody}">

                                    <TextBox Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                    <TextBox Text="{Binding Answer.Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                    <WrapPanel>
                                        <Label Content="Pts:"/>
                                        <TextBox Text="{Binding Points, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             Width="30"/>
                                    </WrapPanel>
                                    <CheckBox Content="Math" IsChecked="{Binding TextIsMath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                                    <Expander Header="Preview" IsExpanded="False">
                                        <CustomControls:QuestionPanel QuestionIndex="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListViewItem}}, Converter={StaticResource IndexConverter}}"
                                                                      Points="{Binding Points}">
                                            <StackPanel Style="{StaticResource QuestionBody}">
                                                <CustomControls:LatexLabel Text="{Binding Text}"
                                                                           IsMath="{Binding TextIsMath}"
                                                                           Margin="0,0,0,10"/>

                                                <TextBox Text="{Binding Answer.Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                         IsEnabled="False"/>
                                            </StackPanel>
                                        </CustomControls:QuestionPanel>
                                    </Expander>
                                    
                                </StackPanel>
                            </CustomControls:QuestionPanel>
                        </DataTemplate>



                        <DataTemplate DataType="{x:Type model:MultipleChoiceQuestion}">

                            <CustomControls:QuestionPanel QuestionIndex="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListViewItem}}, Converter={StaticResource IndexConverter}}">
                                <StackPanel Style="{StaticResource QuestionBody}">
                                    <TextBox Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                    
                                    <ItemsControl ItemsSource="{Binding PossibleAnswers}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type model:Answer}">
                                                <WrapPanel>
                                                    <RadioButton GroupName="{Binding RelativeSource={RelativeSource AncestorType=ItemsControl}, Path=DataContext.UniqueId}"
                                                                 IsChecked="{Binding IsCorrect, Mode=TwoWay}" />
                                                    <TextBox Text="{Binding Path=Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                                </WrapPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <Button Content="+"
                                            Command="{Binding AddPosAnsCommand}"/>
                                    <WrapPanel>
                                        <Label Content="Pts:"/>
                                        <TextBox Text="{Binding Points, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             Width="30"/>
                                    </WrapPanel>
                                    <CheckBox Content="Math" IsChecked="{Binding TextIsMath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                                    <Expander Header="Preview" IsExpanded="False">


                                        <CustomControls:QuestionPanel QuestionIndex="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListViewItem}}, Converter={StaticResource IndexConverter}}"
                                                                      Points="{Binding Points}">
                                            <StackPanel Style="{StaticResource QuestionBody}">
                                                <CustomControls:LatexLabel Text="{Binding Text}"
                                                                           IsMath="{Binding TextIsMath}"
                                                                           Margin="0,0,0,10"/>

                                                <ItemsControl ItemsSource="{Binding PossibleAnswers}"
                                                              Margin="2">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <RadioButton IsChecked="{Binding IsCorrect}"
                                                                         IsEnabled="False">
                                                                <CustomControls:LatexLabel Text="{Binding Text}"
                                                                                           IsMath="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListViewItem}}, Path=DataContext.TextIsMath}"/>
                                                            </RadioButton>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </CustomControls:QuestionPanel>
                                        
                                    </Expander>

                                </StackPanel>
                            </CustomControls:QuestionPanel>

                        </DataTemplate>
                        <DataTemplate DataType="{x:Type model:MultipleChoiceManyQuestion}">

                            <CustomControls:QuestionPanel QuestionIndex="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListViewItem}}, Converter={StaticResource IndexConverter}}">
                                <StackPanel Style="{StaticResource QuestionBody}">
                                    <TextBox Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                                    <ItemsControl ItemsSource="{Binding PossibleAnswers}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type model:Answer}">
                                                <WrapPanel>
                                                    <CheckBox IsChecked="{Binding IsCorrect, Mode=TwoWay}" />
                                                    <TextBox Text="{Binding Path=Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                                </WrapPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <Button Content="+"
                                            Command="{Binding AddPosAnsCommand}"/>
                                    <WrapPanel>
                                        <Label Content="Pts:"/>
                                        <TextBox Text="{Binding Points, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             Width="30"/>
                                    </WrapPanel>
                                    <CheckBox Content="Math" IsChecked="{Binding TextIsMath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                                    <Expander Header="Preview" IsExpanded="False">


                                        <CustomControls:QuestionPanel QuestionIndex="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListViewItem}}, Converter={StaticResource IndexConverter}}"
                                                                      Points="{Binding Points}">
                                            <StackPanel Style="{StaticResource QuestionBody}">
                                                <CustomControls:LatexLabel Text="{Binding Text}"
                                                                           IsMath="{Binding TextIsMath}"
                                                                           Margin="0,0,0,10"/>

                                                <ItemsControl ItemsSource="{Binding PossibleAnswers}"
                                                              Margin="2">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <CheckBox IsChecked="{Binding IsCorrect}"
                                                                      IsEnabled="False">
                                                                <CustomControls:LatexLabel Text="{Binding Text}"
                                                                                           IsMath="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListViewItem}}, Path=DataContext.TextIsMath}"/>
                                                            </CheckBox>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </CustomControls:QuestionPanel>

                                    </Expander>

                                </StackPanel>
                            </CustomControls:QuestionPanel>
                        </DataTemplate>
                        -->
                    </ListView.Resources>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <CustomControls:QuestionEditPanel 
                                Question="{Binding}"
                                IsInEditMode="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListViewItem}}"
                                QuestionIndex="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListViewItem}}, Converter={StaticResource IndexConverter}}"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </ScrollViewer>
    </DockPanel>
</UserControl>
